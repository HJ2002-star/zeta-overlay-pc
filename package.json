{
  "name": "zeta-overlay-pc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "esbuild src/offscreen-src.js --bundle --format=iife --platform=browser --outfile=dist/offscreen.bundle.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esbuild": "^0.28.2",
    "firebase": "^12.19.0"
  }
}
